import { IComponentConfiguration } from "../interfaces/i-component-configuration";
export declare const generateComponentString: (config: IComponentConfiguration, globalResources?: string[]) => string;
