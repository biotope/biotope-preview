import { IComponentConfiguration } from "../interfaces/i-component-configuration";
export declare const generateComponentHtml: (config: IComponentConfiguration, globalResources?: string[]) => string;
